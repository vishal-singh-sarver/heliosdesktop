VITE_BACKEND_URL=http://127.0.0.1:8008
# Set to "true" to serve Geometry from the in-memory mock instead of the
# backend (the scenario-scoped geometry endpoints are not built yet).
VITE_USE_MOCK=true